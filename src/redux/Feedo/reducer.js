import * as types from './actionTypes';

const initialState = {
};

export default function feedoData(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
