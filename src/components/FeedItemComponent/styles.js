import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  thumbnailsView: {
    width: '100%',
    height: 135,
    marginBottom: 6,
  },
  thumbnails: {
    width: '100%',
    height: '100%',
    borderRadius: 10,
  }
})

export default styles
