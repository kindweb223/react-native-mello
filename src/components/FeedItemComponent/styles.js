import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  thumbnails: {
    width: '100%',
    height: 135,
    backgroundColor: '#f00',
    marginBottom: 6,
  }
})

export default styles
