import { StyleSheet, Platform } from 'react-native'

const styles = StyleSheet.create({
	container: {
		flex: 1,
	},
	imageWrapper: {
		overflow: 'hidden',
		position: 'absolute'
	},
	image: {
		position: 'absolute',
	},
	cropContainer: {
		position: 'absolute'
	}
});